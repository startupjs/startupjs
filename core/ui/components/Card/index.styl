.root
  padding 2u
  radius()

  &.elevated
    background-color: var(--color-bg-main-strong)

  &.outlined
    border-width 1px
    border-color var(--color-border-main)

:export
  shadows: $UI.shadows
