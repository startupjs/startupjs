import { createPlugin } from 'startupjs/registry'

export default createPlugin({
  name: 'module-1-plugin',
  server: ({ thing }) => ({
    api (expressApp) {
      console.log('module-1-plugin', thing)
    }
  })
})
