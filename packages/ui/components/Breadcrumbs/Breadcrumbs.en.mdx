import { useState } from 'react'
import { View } from 'react-native'
import Breadcrumbs from '../Breadcrumbs'
import Br from '../Br'
import { Sandbox } from '@startupjs/docs'
import { faRocket, faBook ,faGift } from '@fortawesome/free-solid-svg-icons'

# Breadcrumbs
Breadcrumbs indicate the current page’s location within a navigational hierarchy and allow users to navigate through the hierarchy.

## Example

Each route object accepts any prop given to the [`Link`](/docs/en/components/Link) component apart from the ones mentioned below.

```jsx
import { Breadcrumbs } from '@startupjs/ui'
```

```jsx example
return (
  <Breadcrumbs
    routes={[
      {
        to: '#startupjs',
        name: 'StartupJs'
      },
      {
        to: '#docs',
        name: 'Docs'
      },
      {
        to: '#component',
        name: 'Component'
      }
    ]}
  />
)
```

## Custom Separator

Separator is `/` by default. It can be changed by passing string to `separator` prop.

```jsx
import { Breadcrumbs } from '@startupjs/ui'
```

```jsx example
return (
  <Breadcrumbs
    separator='>'
    routes={[
      {
        to: '#startupjs',
        name: 'StartupJs'
      },
      {
        to: '#docs',
        name: 'Docs'
      },
      {
        to: '#component',
        name: 'Component'
      }
    ]}
  />
)
```

## Icons

Each route object accepts its own icon in `icon` property. Position of icon can be changed by passing `iconPosition` to component (`left` by default).

```jsx
import { Breadcrumbs } from '@startupjs/ui'
import { faHouseUser, faTable } from '@fortawesome/free-solid-svg-icons'
```

```jsx example
return (
  <Breadcrumbs
    separator='>'
    routes={[
      {
        to: '#startupjs',
        name: 'StartupJs',
        icon: faRocket
      },
      {
        to: '#docs',
        name: 'Docs',
        icon: faBook
      },
      {
        to: '#component',
        name: 'Component',
        icon: faGift
      }
    ]}
  />
)
```

## Sizes

Size is `m` by default. It can be changed by changing `size` prop. Possible values of property can be found in the `Sandbox` section at the bottom of page.

```jsx
import { Breadcrumbs } from '@startupjs/ui'
```

```jsx example
return (
  <View>
    <Breadcrumbs
      size='l'
      routes={[
        {
          to: '#startupjs',
          name: 'StartupJs'
        },
        {
          to: '#docs',
          name: 'Docs'
        },
        {
          to: '#component',
          name: 'Component'
        }
      ]}
    />
    <Br />
    <Breadcrumbs
      size='s'
      routes={[
        {
          to: '#startupjs',
          name: 'StartupJs'
        },
        {
          to: '#docs',
          name: 'Docs'
        },
        {
          to: '#component',
          name: 'Component'
        }
      ]}
    />
  </View>
)
```

## Sandbox

<Sandbox Component={Breadcrumbs} />
