.sidebar
  padding 2u
  height 20u
  background-color: $UI.colors.dark

  &-text
    color: $UI.colors.white

.content
  padding 2u
  height 20u
  background-color: $UI.colors.darkLightest

.button
  width 10u

.renderer
.simpleExample
.positionExample
.lockedExample
.visibilityExample
  overflow hidden

.simpleExample
  +native()
    min-height 4u

.positionExample
  +native()
    min-height 27u

.visibilityExample
  +native()
    min-height 16u

.lockedExample
  +native()
    min-height 8u

.renderer
  +native()
    min-height 20u
