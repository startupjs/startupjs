_gutter = $UI.gutters.m
_bgColor = $UI.colors.white

.root
  background-color _bgColor
  radius()

.title
  padding _gutter

  +web()
    &:hover
      cursor pointer

.titleText
  margin-right 1u

.content
  padding 0 _gutter _gutter
