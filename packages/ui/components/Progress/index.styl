_size = 0.5u

.title
  margin-bottom _size
  color $UI.Progress.textColor

.progress
  height _size
  background-color $UI.Progress.bgColor
  border-radius _size

.filler
  height _size
  border-radius _size

.label
  margin-top _size
  color $UI.Progress.textColor
