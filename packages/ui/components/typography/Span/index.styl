_variants = ('default' 'h1' 'h2' 'h3' 'h4' 'h5' 'h6' 'description')

// Add font-family when we add fonts to app

.root
  for variant in _variants
    &.{variant}
      text(variant)

  fontFamily($UI.fontNormal)

  &.bold
    fontFamily($UI.fontNormal, 700)
  &.italic
    fontFamily($UI.fontNormal, 400, italic)
  &.bold.italic
    fontFamily($UI.fontNormal, 700, italic)

  &.header
    fontFamily($UI.fontHeading)
    &.bold
      fontFamily($UI.fontHeading, 700)
    &.italic
      fontFamily($UI.fontHeading, 400, italic)
    &.bold.italic
      fontFamily($UI.fontHeading, 700, italic)