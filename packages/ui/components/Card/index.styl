.root
  padding 2u
  radius()

  &.elevated
    background-color: var(--colors-white, $UI.colors.white)

  &.outlined
    border-width 1px
    border-color var(--colors-darkLighter, $UI.colors.darkLighter)

:export
  shadows: $UI.shadows
