_iconMargin = 0.5u

.root
  padding 0 1u
  height 2u
  min-width @height

  &.circle
    radius(circle)

  &.rounded
    radius()

.iconWrapper
  &.with-label
    &.left
      margin-right _iconMargin
    &.right
      margin-left _iconMargin
