import Link from '../Link'
import Span from '../Typography/Span'
import Div from '../Div'
import Row from '../Row'
import { Sandbox } from '@startupjs/docs'

# Link (ссылка)

Наследует [Span props](/docs/en/components/Span).

Ссылка позволяет перемещаться по приложению и настраивать якоря.

## Пример

```jsx
import { Link, Span } from '@startupjs/ui'
```

```jsx example
return (
  <Span>
    Наследует&nbsp;
    <Link to='/docs/ru/components/Span'>
      Span props
    </Link>
  </Span>
)
```

## Выделяющаяся ссылка

Если ссылка должна выделиться, передайте строку `primary` в свойство `variant`.

```jsx
import { Link, Span } from '@startupjs/ui'
```

```jsx example
return (
  <Row>
    <Span>Inherits&nbsp;</Span>
    <Link block to='/docs/ru/components/Span' variant='primary'>
      <Span>Span props</Span>
    </Link>
  </Row>
)
```

## Sandbox

<Sandbox Component={Link} />
