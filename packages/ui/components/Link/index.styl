.root
  color: var(--colors-mainText, $UI.colors.mainText)
  text-decoration underline

  &.primary
    color: var(--colors-primary, $UI.colors.primary)
    text-decoration-color @color

  &.block
    display flex
    text-decoration none
