import { useState } from 'react'
import { Text } from 'react-native'
import Tooltip from '../Tooltip'
import Button from '../Button'

# Tooltip

```jsx
import { Tooltip } from '@startupjs/ui'
```

## Пример
```jsx example
return (
  <Tooltip>
    <Tooltip.Caption>
      <Text style={{ backgroundColor: '#333' }}>Выйти</Text>
    </Tooltip.Caption>
    <Tooltip.Content style={{ padding: 16 }}>
      Test tip
    </Tooltip.Content>
  </Tooltip>
)
```
