import { useState } from 'react'
import { pug } from 'startupjs'
import FilePicker from '../'
import { useValue } from 'startupjs'
import { Sandbox } from '@startupjs/docs'

# FilePicker

FilePicker позволяет выбирать  файлы для последующих манипуляций с ними

```jsx
import { FilePicker } from '@startupjs/ui'
```

<Sandbox Component={FilePicker} block />