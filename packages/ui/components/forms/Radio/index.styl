_size = 2u

.root
  flex-direction row
  align-items center
  padding 0.5u

.circle
  radius(circle)
  border-width 0.25u
  justify-content center
  align-items center
  width _size
  height @width

  &.with-label
    margin-right 1u

.checked
  radius(circle)
  width (_size / 2)
  height @width
