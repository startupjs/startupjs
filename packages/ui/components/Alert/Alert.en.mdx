import { useState } from 'react'
import Alert from '../Alert'
import Div from '../Div'
import Br from '../Br'
import Button from '../Button'
import { Sandbox } from '@startupjs/docs'
import {
  faCheck,
  faExclamationTriangle
} from '@fortawesome/free-solid-svg-icons'

# Alert

An alert displays a short, important message in a way that attracts the user's attention

## Basic Example

```jsx
import { Alert, Div, Br } from '@startupjs/ui'
```

```jsx example
return (
  <Div>
    <Alert
      label='Primary color alert'
    />
    <Br />
    <Alert
      color='success'
      label='Success alert'
    />
    <Br />
    <Alert
      color='warning'
      label='Warning alert'
    />
    <Br />
    <Alert
      color='error'
      label='Error alert'
    />
  </Div>
)
```

## Alert with Icon

```jsx
import { Alert, Div, Br } from '@startupjs/ui'
```

```jsx example
return (
  <Div>
    <Alert
      icon={faCheck}
      color='success'
      label='Success alert icon'
    />
    <Br />
    <Alert
      icon={faExclamationTriangle}
      color='warning'
      label='Warning alert icon'
    />
  </Div>
)
```

## Alert Action Example

```jsx
import { Alert, Div, Br, Button } from '@startupjs/ui'
```

```jsx example
const [showAlert, setShowAlert] = useState(true)
return (
  <Div>
    {showAlert &&
      <Alert
        color='primary'
        label='Alert with actions'
        onClose={() => setShowAlert(false)}
      />
    }
    <Br />
    <Button disabled={showAlert} onPress={() => setShowAlert(true)}>
      Re-Open
    </Button>
  </Div>
)
```

## Sandbox

<Sandbox Component={Alert} />
