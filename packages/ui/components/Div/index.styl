.root
  +web()
    &.clickable
      &.interactive
        &:hover
          // using important to prevent TouchableOpacity default behavior
          opacity $UI.Div.hoverOpacity !important

        &:active
          // using important to prevent TouchableOpacity default behavior
          opacity $UI.Div.activeStateOpacity !important
