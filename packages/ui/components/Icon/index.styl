$this = merge({
  defaultColor: $UI.colors.dark
}, $UI.Icon, true)

.icon

  &.primary
    color: $UI.colors.primary

  &.success
    color $UI.colors.success

  &.orange
    color $UI.colors.additional0

:export
  config: $this
