import { useState } from 'react'
import Loader from '../Loader'
import Row from '../Row'
import { Sandbox } from '@startupjs/docs'

# Loader

Loaders are used to show a wait time.

```jsx
import { Loader } from '@startupjs/ui'
```

## Example

```jsx example
return (
  <Loader />
)
```

## Sizes

Loaders can be of two sizes - `s` and `m`.

```jsx example
return (
  <Row align='around'>
    <Loader size='s' />
    <Loader size='m' />
  </Row>
)
```

## Colors

Color can be changed by providing hex color or name of the color from `ui` config colors to `color` property.

```jsx example
return (
  <Row align='around'>
    <Loader color='#2962FF' />
    <Loader color='#F5BE59' />
    <Loader color='#00AED6' />
    <Loader color='#A6D800' />
  </Row>
)
```

## Sandbox

<Sandbox Component={Loader} />
