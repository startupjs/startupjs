$text = var(--color-text-on-bg)

.root
  padding 30px
  flex 1
  justify-content center

  &.center
    align-items center

.title
  font-size 32px
  text-align center
  font-weight 600
  margin-bottom 24px
  color $text

.description
  font-size 16px
  text-align center
  color $text
  margin-bottom 16px
