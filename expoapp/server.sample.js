// This is an example server file for production.
// It's provided just for a reference and is NOT actually being used
// to start the server.
// That's because the `package.json/start-production` script runs this internally.
await import('startupjs/nodeRegister')
await import('startupjs/startServer')
